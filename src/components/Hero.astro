---
import { url } from '../utils/links';

interface Props {
  title: string;
  subtitle?: string;
  showCTA?: boolean;
  backgroundImage?: string;
}

const { 
  title, 
  subtitle, 
  showCTA = false,
  backgroundImage = '/images/pwic-background-traditional.jpeg'
} = Astro.props;

const bgImage = backgroundImage.startsWith('/') ? url(backgroundImage) : backgroundImage;
---

<section class="relative min-h-[70vh] flex items-center justify-center overflow-hidden">
  <!-- Background Image -->
  <div class="absolute inset-0">
    <img 
      src={bgImage} 
      alt="PWIC Mosque" 
      class="w-full h-full object-cover"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-primary-900/90 via-primary-800/80 to-primary-900/70"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white font-heading leading-tight mb-6">
      {title}
    </h1>
    {subtitle && (
      <p class="text-xl sm:text-2xl text-primary-100 max-w-3xl mx-auto mb-8 leading-relaxed">
        {subtitle}
      </p>
    )}
    {showCTA && (
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href={url('/about')} 
          class="px-8 py-4 bg-white text-primary-700 rounded-lg font-semibold hover:bg-primary-50 transition-colors shadow-lg hover:shadow-xl"
        >
          Learn More
        </a>
        <a 
          href={url('/donate')} 
          class="px-8 py-4 bg-primary-500 text-white rounded-lg font-semibold hover:bg-primary-400 transition-colors shadow-lg hover:shadow-xl border-2 border-primary-400"
        >
          Support Our Mission
        </a>
      </div>
    )}
  </div>

  <!-- Decorative Islamic Pattern (subtle) -->
  <div class="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-white to-transparent"></div>
</section>
