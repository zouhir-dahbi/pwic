---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import ServiceCard from '../components/ServiceCard.astro';
import services from '../data/services.json';
import { url } from '../utils/links';
---

<Layout title="Services">
  <Hero 
    title="Our Services"
    subtitle="How we serve our community"
    backgroundImage={url('/images/mosque-entrance-panel.jpeg')}
  />

  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {services.map((service) => (
          <ServiceCard 
            title={service.title}
            description={service.description}
            icon={service.icon}
          />
        ))}
      </div>
    </div>
  </section>

  <!-- Request Services -->
  <section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-primary-50 rounded-2xl p-8 md:p-12 text-center">
        <h2 class="text-2xl md:text-3xl font-bold text-primary-800 font-heading mb-4">
          Need to Request a Service?
        </h2>
        <p class="text-gray-600 mb-8 max-w-2xl mx-auto">
          For Nikah ceremonies, funeral services, or other religious services, 
          please contact us to schedule an appointment with our Imam.
        </p>
        <a 
          href={url('/contact')}
          class="inline-flex items-center gap-2 px-8 py-4 bg-primary-600 text-white rounded-xl font-semibold hover:bg-primary-700 transition-colors shadow-lg"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
          Contact Us
        </a>
      </div>
    </div>
  </section>

  <!-- Additional Information -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-2 gap-8">
        <div class="bg-white rounded-xl p-8 shadow-lg">
          <h3 class="text-xl font-semibold text-gray-800 mb-4">Jummah Prayer</h3>
          <p class="text-gray-600 mb-4">
            Join us every Friday for Jummah prayer. The khutbah begins at 1:00 PM.
          </p>
          <ul class="text-sm text-gray-500 space-y-2">
            <li class="flex items-center gap-2">
              <svg class="w-5 h-5 text-primary-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span>Time: 1:00 PM</span>
            </li>
            <li class="flex items-center gap-2">
              <svg class="w-5 h-5 text-primary-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              <span>Arrive early for best seating</span>
            </li>
          </ul>
        </div>

        <div class="bg-white rounded-xl p-8 shadow-lg">
          <h3 class="text-xl font-semibold text-gray-800 mb-4">Daily Prayers</h3>
          <p class="text-gray-600 mb-4">
            Our masjid is open for all five daily prayers. Check our prayer times for the current schedule.
          </p>
          <a 
            href={url('/')}
            class="inline-flex items-center gap-2 text-primary-600 font-medium hover:text-primary-700"
          >
            View Prayer Times
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
